# Runtime
fastapi>=0.110
uvicorn>=0.27
httpx>=0.26
pydantic>=2.0

# Projection model and backtest (ffopt/projections). The app runs without
# them; the model and backtest are skipped.
nflreadpy>=0.1
polars>=1.0
pyarrow>=15.0
scikit-learn>=1.4

# Tests
pytest>=7.4
beautifulsoup4>=4.12
anthropic>=0.40
